<p id="notice"><%= notice %></p>

<p>
    <% @url = @reference.URL %>
    <strong>URL:</strong>
    <a href="<%= @reference.URL %>" target="_blank"><%= @url %></a>
</p>

<p>
    <strong>Topic:</strong>
    <%= @reference.topic %>
</p>

<p>
    <strong>Annotation:</strong>
    <%= @reference.annotation %>
</p>
<p>
    <strong>DateTime:</strong>
    <%= @reference.created_at %>
<br>    
<br>
<%= link_to 'Edit', edit_reference_path(@reference) %>
<%= link_to 'Destroy', @reference, method: :delete, data: { confirm: "Are you sure you want to delete #{@reference.topic} reference?" } %>
<br>
<br>
<%= link_to 'Show All High Scores', references_path %>
